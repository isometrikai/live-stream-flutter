import 'package:flutter/material.dart';

class IsmLiveStreamView extends StatelessWidget {
  const IsmLiveStreamView({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(
          child: Text('IsmLive Home View'),
        ),
      );
}
