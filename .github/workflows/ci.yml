name: CI

on:
  push:
    branches: [main, main_fix]
  pull_request:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze-test:
    name: Analyze & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      # Existing codebase has many info/warning lints; gate on errors only for now.
      - name: Analyze
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Test
        run: flutter test

  build-example-android:
    name: Build example (Android)
    runs-on: ubuntu-latest
    needs: analyze-test
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install example dependencies
        working-directory: example
        run: flutter pub get

      - name: Build debug APK
        working-directory: example
        # secrets.json.example has empty placeholders; enough to compile.
        # Real runs use --dart-define-from-file=secrets.json (gitignored).
        run: flutter build apk --debug --dart-define-from-file=secrets.json.example
